<turbo-frame id="trip_sheet_form">
	<div class='border'>
		{{ form_start(form, {'action': path('trip_sheet_create')}) }}
		{{ form_widget(form) }}
		<button type="submit">Создать</button>
		{{ form_end(form) }}


	</div>


</turbo-frame>
